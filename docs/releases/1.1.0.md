# vcf-sv-stats 1.1.0

This release updates the distribution and performance qualification contract.
The release artifact set is exactly one universal `py3-none-any` wheel and a
`SHA256SUMS` file for that wheel. No benchmark result is claimed until the
integrated candidate has been measured.

`--threads` enables HTSlib/BGZF compressed-input decompression concurrency.
Python semantic scanning and cross-record relationship resolution remain serial.
